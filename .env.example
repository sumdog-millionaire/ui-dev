# Copy to .env (gitignored). The Stitch MCP server holds its own key in the Claude Code MCP config;
# this one is read only by the upload script, which posts images and HTML to Stitch over REST.
STITCH_API_KEY=
